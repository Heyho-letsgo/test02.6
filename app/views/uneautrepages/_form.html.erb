<%= form_for(@uneautrepage) do |f| %>
  <% if @uneautrepage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@uneautrepage.errors.count, "error") %> prohibited this uneautrepage from being saved:</h2>

      <ul>
      <% @uneautrepage.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :civilite %><br>
    <%= f.text_field :civilite %>
  </div>
  <div class="field">
    <%= f.label :prenom %><br>
    <%= f.text_field :prenom %>
  </div>
  <div class="field">
    <%= f.label :nom %><br>
    <%= f.text_field :nom %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
